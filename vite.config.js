import { defineConfig } from 'vite';

export default defineConfig({
  // Set the base URL for your project when deployed to GitHub Pages.
  // Replace 'YOUR_REPO_NAME' with the actual name of your GitHub repository.
  // For example, if your repo is 'my-awesome-app', set base: '/my-awesome-app/'
  base: '/statblock-converter/', 
});